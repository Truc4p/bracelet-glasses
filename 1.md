```mermaid
erDiagram
    FRAME {
        string id PK
        string name
        string dimensions
        string description
        string array_images
        datetime createdAt
        datetime updatedAt
    }

    IMAGE {
        string filename
        buffer data
        string contentType
        datetime createdAt
        datetime updatedAt
    }

    SHADE {
        string baseColor PK
        map lenses
        datetime createdAt
        datetime updatedAt
    }

    FRAME ||--o{ IMAGE : references_by_filename
 ``` 